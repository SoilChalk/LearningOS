protocol_version: 20

task_id: task-003-gate-3-state-persistence
status: ready
branch: agent/task-003-state-persistence
result_file: agent-control/results/task-003.json

objective: >
  Implement the minimal learning-state save/load/validation capability required
  by FIRST_VERTICAL_SCENARIO Step 6 (State Persistence): a Python module that
  (a) saves a valid learning-state instance as JSON, (b) loads it back with
  round-trip equivalence, and (c) validates instances against the existing
  templates/MINIMAL_LEARNING_STATE.schema.json (JSON Schema Draft-07; the 11
  declared required fields; additionalProperties: false; evidence_level enum
  0|1|2). Reuse the existing validation infrastructure and schema unchanged.
  Do NOT redesign the schema, add a learner model, a database, a scheduler, or
  any new persistence abstraction.

owner_authorization:
  recorded_at: "2026-08-09T20:40:00+08:00"
  source_instruction: "Owner authorization — Gate 3 Minimum Viable Pilot start; first task A (State Persistence slice)"
  scope: >
    Implement minimal state save/load/validation for the single vertical
    scenario. This is the FIRST implementation slice of Gate 3, NOT Gate 3
    completion. Do not claim a complete learning session, a completed pilot,
    validated LearningOS architecture, or a validated learner model. No learner
    model, no DB, no scheduler, no new persistence abstraction, no schema
    redesign.

allowed_paths:
  - scripts/learning_state.py
  - scripts/test_learning_state.py
  - agent-control/results/task-003.json

forbidden_paths:
  - templates/**
  - sources/**
  - docs/**
  - state/**
  - agent-control/CURRENT_TASK.yaml
  - agent-control/TASK_PROTOCOL.md
  - agent-control/VALIDATION_RECORD.txt
  - agent-control/results/task-001*.json
  - agent-control/results/task-002*.json
  - scripts/validate_task_002.py
  - scripts/test_task_002_negative.sh
  - scripts/check_control_state.py
  - scripts/test_control_state.sh
  - scripts/test_validation_negative.sh
  - scripts/validate_source_records.py
  - AGENTS.md
  - README.md
  - .github/**

acceptance_commands:
  - python3 -m py_compile scripts/learning_state.py
  - python3 scripts/test_learning_state.py
  - python3 scripts/validate_task_002.py
  - bash scripts/test_task_002_negative.sh
  - python3 -m json.tool agent-control/results/task-003.json >/dev/null

completion_conditions:
  - All acceptance commands exit 0.
  - A valid complete state instance can be saved and reloaded with round-trip
    equivalence.
  - Missing required field is rejected.
  - Unknown/additional field is rejected.
  - evidence_level outside [0,1,2] is rejected.
  - Malformed state fails explicitly (no silent repair).
  - Existing Gate 2 validation remains green (validate_task_002.py and
    test_task_002_negative.sh unchanged and passing).

stop_conditions:
  - Two consecutive tool failures (per TASK_PROTOCOL.md).
  - A declared blocker prevents safe completion.

inputs:
  - templates/MINIMAL_LEARNING_STATE.schema.json (read-only, do not modify)
  - docs/FIRST_VERTICAL_SCENARIO.md (read-only context for Step 6 semantics)
